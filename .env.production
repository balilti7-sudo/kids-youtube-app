# Production Media Bridge (HTTPS origin only — never the Vercel/frontend URL).
# Ephemeral *.trycloudflare.com tunnels expire; use Render or a named Cloudflare Tunnel.
VITE_STREAM_API_BASE=https://safetube-media-bridge.onrender.com
