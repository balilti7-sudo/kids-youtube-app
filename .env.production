# Self-hosted Media Bridge — explicit HTTP + public IPv4 + port 3001.
# WARNING: If the frontend is served over HTTPS (e.g. Vercel), browsers will block
# this mixed-content fetch. Either serve the frontend over HTTP too, or put HTTPS
# (e.g. Caddy / Cloudflare Tunnel / nginx + Let's Encrypt) in front of the bridge.
VITE_STREAM_API_BASE=http://176.9.82.81:3001
