# C:\ProgramData\SafeTube\bridge.env
#
# Production env for the SafeTube Media Bridge on the Windows server.
# Loaded by start-bridge.ps1 at service start.
#
# Install:
#   install.ps1 copies this file to C:\ProgramData\SafeTube\bridge.env on first run.
#   To edit:  notepad C:\ProgramData\SafeTube\bridge.env
#   To apply: Restart-Service SafeTubeBridge
#
# IMPORTANT: Backslashes are LITERAL in this file -- do NOT double them.
# Use   C:\Users\me\file.txt    (not  C:\\Users\\me\\file.txt).
#
# NEVER commit a filled-in bridge.env to git -- it contains secrets.

# --- core bridge config -------------------------------------------------------
PORT=3001
MEDIA_BRIDGE_HOST=0.0.0.0

# CORS: production frontend origin(s), comma-separated.
MEDIA_BRIDGE_CORS_ORIGINS=https://www.safetube.co.il,https://safetube.co.il
# Or, for testing only, allow any origin (NOT recommended on a public IP):
# MEDIA_BRIDGE_CORS_ALLOW_ANY=1

# Public origin used inside HLS manifests. Set to your Cloudflare Tunnel URL (no trailing slash).
# Example: PUBLIC_BASE_URL=https://safetube-bridge.trycloudflare.com
PUBLIC_BASE_URL=FIXME_PASTE_YOUR_CLOUDFLARE_TUNNEL_URL_HERE

# --- yt-dlp auth: PO token + visitor_data (rotated every 4h by Task Scheduler) -
# These two lines are MANAGED AUTOMATICALLY by refresh-pot.ps1.
# Leave them empty initially -- the first scheduled task run will populate them.
YOUTUBE_PO_TOKEN=
YOUTUBE_VISITOR_DATA=

# Optional: Netscape cookie file for yt-dlp (export from browser; path relative to server/).
# Default in bridge code is ./youtube_cookies.txt if present.
YT_DLP_COOKIES_FILE=./youtube_cookies.txt

# --- Supabase (required if REQUIRE_CONFIRMED_EMAIL_FOR_STREAM=1) --------------
SUPABASE_URL=https://ioylyyqlluenkkltguhf.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImlveWx5eXFsbHVlbmtrbHRndWhmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzYxMDM1NDEsImV4cCI6MjA5MTY3OTU0MX0.4yisWFVx7wiln2ZszL7crBry0hazyXSEnzchcXiBaes
REQUIRE_CONFIRMED_EMAIL_FOR_STREAM=1

# --- Playback grant signing --------------------------------------------------
# NOTE: SuperSecret123456789 is weak. For production, generate a strong secret with:
#   -join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Maximum 256) })
MEDIA_BRIDGE_GRANT_SECRET=SuperSecret123456789
STREAM_GRANT_TTL_MS=900000

# --- Resend + Supabase (required for /api/email/pin-changed after PIN update) ---
# RESEND_API_KEY=re_xxxxxxxx
# RESEND_FROM=SafeTube <support@safetube.co.il>
# SUPABASE_URL and SUPABASE_ANON_KEY must match the SafeTube app (Bearer auth on pin-changed).
# MEDIA_BRIDGE_WELCOME_KEY=FIXME_match_VITE_MEDIA_BRIDGE_WELCOME_KEY

# --- Outbound: direct egress from the German Windows IP -- no proxy -----------
OUTBOUND_PROXY_DISABLE=1

# --- yt-dlp tuning ------------------------------------------------------------
# InnerTube clients (tv + web_safari). Override only if YouTube changes requirements.
YT_DLP_PRIMARY_EXTRACTOR_ARGS=youtube:player_client=tv,web_safari
# Rust bgutil POT HTTP server (install separately; see deploy/windows-server/README.md).
YT_DLP_BGUTIL_POT_BASE_URL=http://127.0.0.1:4416
# Prefer merged ≤720p MP4; avoids heavily throttled fmt 18.
YT_DLP_FORMAT=bestvideo[height<=720][ext=mp4]+bestaudio[ext=m4a]/best[height<=720][ext=mp4]/best
# Extra yt-dlp plugin dirs (semicolon-separated), relative to server/ unless absolute.
# YT_DLP_PLUGIN_DIRS=
# YT_DLP_PER_ATTEMPT_TIMEOUT_MS=30000
# OVERALL_RESOLVE_BUDGET_MS=120000
